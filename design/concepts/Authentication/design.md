I updated the concept to clarify that hashed passwords were being stored in the state for security, which is an important detail to add.

In the actual implementation, I added two helper functions to convert between user IDs and usernames for convenience that I do not include in the concept specification since the IDs are not actually part of the concept.